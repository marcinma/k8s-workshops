apiVersion: apps/v1
kind: Deployment
metadata:
  name: canary-deployment-old
  labels:
    app: cnary-myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cnary-myapp
  template:
    metadata:
      name: cnary-myapp-pod
      labels:
        app: cnary-myapp
    spec:
      containers:
      - name: myapp-container
        image: ewoutp/docker-nginx-curl
        ports:
          - containerPort: 80